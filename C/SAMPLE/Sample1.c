#include <stdio.h>
#include <stdlib.h>

int main(void)
{
  int sum;
  sum = 30 + 40 + 50 + 60 + 70;
  printf("sum : %d\n", sum);
  printf("sum : %d\n", sizeof(sum));
  return EXIT_FAILURE;
}
